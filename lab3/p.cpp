#include <bits/stdc++.h>

using namespace std;

int main()
{
   long long a;
   cin>>a;
   double x=sqrt(a);
   int y=sqrt(a);
   if(x==y)
    {
        cout<<"YES";
    }
    else if (x!=y)
    {
        cout<<"NO";
    }
}
